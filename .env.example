# GoHighLevel / LeadConnector MCP server
# Private Integration Token (pit-...) used by the `ghl` server in .mcp.json.
# Copy this file to `.env`, fill in your real token, and NEVER commit `.env`.
# Generate/rotate the token in GHL: Settings -> Private Integrations.
GHL_MCP_TOKEN=pit-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
